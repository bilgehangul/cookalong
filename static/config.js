// Where the API lives.
//   ""                         -> same origin (local dev, or a single-service deploy)
//   "https://<space>.hf.space" -> the Hugging Face Space backend (Firebase Hosting deploy)
// deploy_firebase.py rewrites this line, so keep the format on one line.
window.COOKALONG_API_BASE = "";
